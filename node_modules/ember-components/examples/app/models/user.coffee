`import DS from 'ember-data'`

User = DS.Model.extend Ember.Validations.Mixin,
    name: DS.attr 'string'
    password: DS.attr 'string'
    nameHasValue: (->
        not @get('name')?.length
    ).property('name')

User.reopen
    validations:
        name:
            presence: true
            length: { minimum: 5 }
        password:
            presence: true
            length: { minimum: 6 }

User.reopenClass
    FIXTURES: [
        {id: 1, name: 'Arek Dolv'},
        {id: 2, name: 'Andy Genty'}
    ]

`export default User`

