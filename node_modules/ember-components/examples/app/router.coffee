`import Ember from 'ember';`

Router = Ember.Router.extend
    location: ExamplesENV.locationType

Router.map ->
    @route 'overview'
    @route 'getstarted'
    @resource 'component', ->
        @resource 'component.forms', ->
        @resource 'component.tabs', ->
            @route 'simple',
            @route 'query_params',
            @route 'controller_per_tab'
        @resource 'component.wysiwyg', ->
            @route 'simple'
        @resource 'component.accordion', ->
            @route 'simple'
            @route 'query_params'
            @route 'controller_per_item',
        @resource 'component.tree', ->
            @route 'simple'
            @route 'emberdata'
            @route 'async'
            @route 'hoveredactions'
            @route 'expand'
            @route 'multiselection'
        @resource 'component.list', ->
            @route 'simple'
            @route 'state'
            @route 'models'
        @resource 'component.modal', ->
            @route 'simple'
            @route 'openifcloseif'
            @route 'confirm'
            @route 'reuse'
            @route 'forms'
            @route 'emforms'
        @resource 'component.button', ->

`export default Router;`
