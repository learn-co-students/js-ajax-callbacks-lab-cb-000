`import Ember from 'ember'`

AsyncRoute = Ember.Route.extend
    model: ->
        #TODO: Import EC instead
        root = Em.EC.TreeNode.create({title: 'Family'})
        root

`export default AsyncRoute`