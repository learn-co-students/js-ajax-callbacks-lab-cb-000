`import Ember from "ember"`

Multi = Ember.Controller.extend
    multi: Ember.A()

    multiNames: (->
        s = ""
        @multi.forEach((r) ->
            s+= " " + r.get('title')
        )
        s
    ).property('multi.length')

`export default Multi`