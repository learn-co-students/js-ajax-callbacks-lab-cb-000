`import Ember from "ember"`

Confirm = Ember.Controller.extend
    open: false
    close: false
    actions:
        confirm: ->
            alert "Confirmed!"
            @set 'close', true
            
`export default Confirm`